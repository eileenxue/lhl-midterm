<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Create Event</title>

    <!-- Bootstrap -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />

    <!-- CSS -->
    <link rel="stylesheet" href="/styles/layout.css" type="text/css" />
    <link rel="stylesheet" href="/styles/create.css" type="text/css" />

    <!-- Scripts -->
    <script>
      function loadNextPage() {
        window.location.href = "http://localhost:8080/options";
      }
    </script>
  </head>

  <body>
    <!-- Refer to header in partials folder -->
    <%- include('partials/_header') %>

    <main class="container mt-4 create-page">
      <h1>Create Event</h1>
      <form class="create-form" method="POST" action="/create">
        <p>👋 Hello, nice to meet you! Who's setting up this event?</p>
        <div class="form-floating mb-3">
          <input
            type="text"
            name="name"
            class="form-control"
            placeholder="Name"
          />
          <label for="name">Name</label>
        </div>
        <div class="form-floating mb-3">
          <input
            type="email"
            name="email"
            class="form-control"
            placeholder="youremail@mail.com"
          />
          <label for="email">Email</label>
        </div>
        <p>✍️ Tell us (and your guests) what the event is about!</p>
        <div class="form-floating mb-3">
          <input
            type="text"
            name="title"
            class="form-control"
            placeholder="Title"
          />
          <label for="title">Event Title</label>
        </div>
        <!-- Location with Google Map Search? -->
        <div class="form-floating mb-3">
          <input
            type="text"
            name="location"
            class="form-control"
            placeholder="Location"
          />
          <label for="Location">Location</label>
        </div>
        <div class="form-floating mb-3">
          <textarea
            type="text"
            name="description"
            class="form-control"
            placeholder="Description"
            style="height: 150px"
          ></textarea>
          <label for="description">Description</label>
        </div>
        <p>
          ⏰ Select your times on the next page and then you're almost done!
        </p>
        <!-- <button class="btn btn-primary" type="button" onclick="loadNextPage()"> -->
        <button class="btn btn-primary" type="submit">Next</button>
      </form>
    </main>

    <!-- Refer to footer in the partials folder -->
    <%- include('partials/_footer') %>

    <!-- Bootstrap JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
